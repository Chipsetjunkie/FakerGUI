import React from "react";
import ReactDOM from "react-dom/client";
import { FakerUI } from "../lib/FakerUI";



ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <FakerUI />
  </React.StrictMode>
);
